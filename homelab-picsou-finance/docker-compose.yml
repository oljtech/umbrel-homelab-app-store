version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: homelab-picsou-finance_server_1
      APP_PORT: 8080

  server:
    image: ghcr.io/zoeille/picsou-finance:1.1.0@sha256:dac5f93d72f2b8ec75fe398fcb846c2c53d78a1bc804722ad220be2072e2241e
    restart: on-failure
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://db:5432/picsou?sslmode=prefer
      SPRING_DATASOURCE_USERNAME: picsou
      # L'entrypoint exporte lui-même SPRING_DATASOURCE_PASSWORD depuis
      # $POSTGRES_PASSWORD (ne pas passer SPRING_DATASOURCE_PASSWORD ici).
      POSTGRES_PASSWORD: ${APP_SEED}
      SPRING_PROFILES_ACTIVE: prod
      TR_AUTH_URL: http://tr-auth:8001
    volumes:
      # /data : secrets auto-générés, clés Enable Banking, clé du wizard
      - ${APP_DATA_DIR}/data/app:/data
      # Déposer enablebanking.pem ici (via SSH) pour activer la synchro bancaire
      - ${APP_DATA_DIR}/data/secrets:/run/secrets:ro
    depends_on:
      - db
      - tr-auth

  # Sidecar d'authentification Trade Republic (port interne 8001)
  tr-auth:
    image: ghcr.io/zoeille/picsou-finance/tr-auth:1.1.0@sha256:6974e38bb231661f4aa0acd79c7811dee7039908a401c2ac18d2ca0f3621b898
    restart: on-failure

  db:
    image: postgres:16-alpine@sha256:57c72fd2a128e416c7fcc499958864df5301e940bca0a56f58fddf30ffc07777
    restart: on-failure
    environment:
      POSTGRES_DB: picsou
      POSTGRES_USER: picsou
      POSTGRES_PASSWORD: ${APP_SEED}
    volumes:
      - ${APP_DATA_DIR}/data/postgres:/var/lib/postgresql/data
